import React from 'react'

const UserLayout = ({ children }) => {
    return (
        <React.Fragment>
            <div>UserLayout</div>
            {children}
        </React.Fragment>
    )
}

export default UserLayout
